language: node_js
node_js:
  - "stable"
services:
  - docker

before_install:
  - docker build -t harjyotbagga/react-test-proj -f Dockerfile.dev .

script: 
  - docker run -e CI=true harjyotbagga/react-test-proj npm run test

# deploy:
#   provider: elasticbeanstalk
#   region: "ap-south-1"
#   app: "docker-react-test-proj"
#   env: "Dockerreacttestproj-env"
#   bucket_name: "elasticbeanstalk-ap-south-1-506024966338"
#   bucket_path: "docker-react-test-proj"
#   on:
#     branch: main
#   access_key_id: $AWS_KEY_ID
#   secret_access_key: $AWS_ACCESS_KEY


# Install / Use Docker -> Build image using Dockerfile.dev -> Run Test Suite -> Deploy code on AWS