sudo: require
language: node_js
node_js:
  - "stable"
services:
  - docker

before_install:
  - docker build -t harjyotbagga/react-project -f Dockerfile.dev .

script:
  - docker run -e CI=true harjyotbagga/react-project npm run test